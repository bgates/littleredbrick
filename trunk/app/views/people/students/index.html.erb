<div id="content">
<h1>Students List</h1>
<%= notice_or_error %>
<p>The list below shows all students in the school and their schedules for the <%= 'next ' if params[:term] %>term. Click the name of a student or section to get more information.</p>
<div class="scrollableTable">
 <table class='sortable resizable' cellspacing ="0">
  <colgroup><col width="15%"/><col width="50"/><col width="50"/></colgroup>
  <thead>
   <%= index_header('Student', @low_period, @high_period) %>
  </thead>
  <tbody>
   <%= render(:partial => "student", :collection => @students) %>
   <%= filler_row(@students, @high_period - @low_period + 4) %>
  </tbody>
 </table>
</div>
<%#= pagination @students %>
</div>
<div id="secondary">
<%= initial %>
<h2>New Student</h2>
<p>To create a new student account, click <%= link_to "here", new_student_path %></p>
<%= term_link %>
</div>
