<noscript>
<% if params[:school] %>
 <h3>Your plan is $<%= params[:school][:teacher_limit] + '0' %>/month</h3>
 <p><span class="highlight">You will be billed at the end of each month.</span> If you keep your account open past that time you&#8217;ll be charged $<%= params[:school][:teacher_limit] + '0' %>/month for the service. If you don&#8217;t want to continue using the service, just cancel before you&#8217;re billed on <%= 1.month.from_now.strftime("%b %d, %Y") %> and you won't be charged.</p>
 <h3>The service is month-to-month, cancel at any time</h3>
 <p>You will not have to pay sign-up fees or cancellation fees, or sign long-term contracts.</p>
 <h3>You will log in at this address</h3>
 <p>http://<%= params[:school][:domain_name] %>.littleredbrick.com</p>
<% else %>
<p>Click here to review the details of the site you are creating.</p>
<p><%= submit_tag("Review Site") %></p>
<% end %>
</noscript>
