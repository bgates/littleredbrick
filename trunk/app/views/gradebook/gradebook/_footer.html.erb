<tfoot>
 <tr>
  <th style="text-align:left"><%= link_to image_tag('add_16_ie.png', {:alt => "Add Student", :title => "Add Student"}), new_section_enrollment_path(@section) %><%= link_to image_tag('sub_16_ie.png', {:alt => 'Remove Student', :title => 'Remove Student'}), section_path(@section, :unenroll => true), :id => 'unenrollment' if @students.length > 0 %></th>
  <th colspan = '<%= [1, @assignments.length].max %>'>
   <%= link_to 'Sort names', sort_section_gradebook_index_path(@section) if @students.length > 1 %>
  </th>
  <th>&nbsp;</th>
  <th colspan="2"><%= submit_tag 'Update Grades', :class => 'button', :disabled => @students.empty? || @assignments.empty? %></th>
 </tr>
</tfoot>
