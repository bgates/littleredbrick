<div id="content">
<h1>Attendance Overview</h1>
<%= notice_or_error %>
<p>The list below shows the attendance record for all students in the school for <%= @date.strftime("%A, %B %d") %>. Click the name of a student to get more information on that student&#8217;s attendance.</p>
<div class="scrollableTable">
 <table class='sortable resizable' cellspacing ="0">
  <colgroup><col width="15%"/><col width="50"/><col width="50"/></colgroup>
  <thead>
   <%= index_header('Student', @low_period, @high_period) %>
  </thead>
  <tbody>
   <%= render(:partial => "attendance", :collection => @students) %>
   <%= filler_row(@students, @high_period - @low_period + 4) %>
  </tbody>
 </table>
</div>
</div>
<div id="secondary">
<h2>Change Date</h2>
<%= form_tag({}, {:method => :get, :id  => 'date_selection'}) do %>
 <fieldset style='padding-top:1em'>
 <p>To see schoolwide attendance for another day, select the date here.</p>
   <%= datebocks_field_tag 'date', params[:date], 'Date ' %>
   <%= submit_tag("Set Date", :class => 'button') %>
 </fieldset>
<% end %>
</div>
